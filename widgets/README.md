# widgets

Composite, self-contained UI blocks that combine features/entities into a section of a page (e.g. `widgets/invoice-list`, `widgets/sidebar-nav`).
May import from `features/`, `entities/`, and `shared/`.
