# shared

Reusable code with no business meaning: UI kit (`ui/`), utilities (`lib/`), hooks (`hooks/`), app-wide config (`config/`).
Nothing here may import from `entities/`, `features/`, `widgets/`, or `app/`.
