import type { TemplateMeta } from "../../model/template";

export const meta: TemplateMeta = {
  templateKey: "beforeafter",
  name: "Before / after",
  category: "content",
  thumbnail: "beforeafter",
  schemaVersion: 1,
};
