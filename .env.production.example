# Template for `.env.production`. Copy to `.env.production` and fill in the
# real deployed API URL — never commit the filled-in file.

BACKEND_API_BASE_URL=https://REPLACE_ME.example.com/api/v1
NEXT_PUBLIC_ASSET_BASE_URL=https://REPLACE_ME.example.com
